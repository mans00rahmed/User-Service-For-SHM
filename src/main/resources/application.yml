server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:h2:mem:usersdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  # Disable default Spring Security login
  security:
    user:
      name: disabled
      password: disabled

app:
  jwt:
    secret: "my-super-secure-jwt-secret-key-2026-smart-home-platform"
    expirySeconds: 3600

eureka:
  client:
    enabled: false